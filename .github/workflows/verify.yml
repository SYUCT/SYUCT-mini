name: Verify

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 校验 JS 语法
        run: node --check app.js

      - name: 运行同步与包体校验
        run: python3 scripts/verify-sync.py
